export const metadata = {
  title: 'ADVISOR Market Intelligence',
  description: 'Market Intelligence Report Engine',
};

export default function RootLayout({ children }) {
  return (
    <html lang="it">
      <body>{children}</body>
    </html>
  );
}
